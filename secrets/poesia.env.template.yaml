# Template only. Do not put real credentials here.
# Encrypted target file name: secrets/poesia.env.sops.yaml
#
# Live values are captured from the gitignored repo-root .env and .env_mlflow.
# The LLM keys (OPENAI/GROQ/GEMINI/REPLICATE) are read from the environment by
# the --llm providers; include them here if you want them portable across
# devices. DATABASE_URL / MLFLOW_TRACKING_URI embed the postgres credentials.

CLOUDFLARE_ACCOUNT_ID: ""
CLOUDFLARE_API_TOKEN: ""
DATABASE_URL: ""
MLFLOW_TRACKING_URI: ""
OPENAI_API_KEY: ""
GROQ_API_KEY: ""
GEMINI_API_KEY: ""
REPLICATE_API_TOKEN: ""
