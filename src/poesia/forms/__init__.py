"""Stanza and poetic form definitions.

Encodes the structural container (line count, rhyme scheme, syllable target
per line) that a generated poem must satisfy. Kept separate from phonology
(which validates individual lines) so forms can be added/edited without
touching scansion logic.
"""
